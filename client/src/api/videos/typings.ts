export type Video = {
  id: string;
  url: string;
  title: string;
  description: string;
  sharedBy: string;
};

export type VideoDetails = {
  videoId: string;
  description: string;
  title: string;
};
